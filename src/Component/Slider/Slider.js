import React from 'react'
import { nextSlide, previousSlide,autoSlide } from '../../features/slices/sliderSlice'
import {useSelector, useDispatch} from "react-redux"
const Slider = () => {
  return (
    <>
   <button>
    
   </button>
   <button>

   </button>
    </>
  )
}

export default Slider